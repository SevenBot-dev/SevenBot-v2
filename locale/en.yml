chat_input:
  ping:
    _:
      name: "ping"
      description: "Pong! Returns the latency of SevenBot."
      args:
    embed:
      title: ":ping_pong: Pong!"
      description: "The latency is `{latency}ms`."